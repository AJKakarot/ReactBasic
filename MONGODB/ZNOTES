// 🔹 With populate:
// const posts = await Post.find().populate("userId");

// 🔥 Output:
// {
//   "title": "Hello",
//   "userId": {
//     "_id": "u1",
//     "name": "Ajeet",
//     "email": "abc@gmail.com"
//   }
// }

// 👉 Behind the scenes:

// Pehle Post.find() chalta hai
// Sab userId collect hote hain
// Fir MongoDB User collection me query karta hai
// Fir replace kar deta hai ID ko full object se

// 👉 Matlab:

// "populate internally ek extra query run karta hai"

// ✅ 4. Select specific fields (Important 🔥)
// Post.find().populate("userId", "name email");

// 👉 Sirf name & email aayega

// ✅ 5. One-to-Many ka flow (Short Revision)
// Parent → User
// Child → Post
// Post me store hota hai → userId
// Populate → full user data laata hai

// 🔥 Interview Golden Lines
// "MongoDB me one-to-many referencing se achieve hota hai"
// "Foreign key jaisa concept → ObjectId + ref"
// "populate() MongoDB ka JOIN alternative hai"
// "It performs additional query under the hood"
// ⚡ Bonus (Kab embed kare?)
// Case	Use
// Small data	Embed
// Large / scalable	Reference ✅


// Axios simplifies HTTP requests with automatic JSON parsing, better error handling, and interceptors compared to fetch."




// 📌 1. Fetch vs Axios (Simple Notes)
// 🔹 Fetch
// Built-in browser API
// JSON manually parse karna padta hai
// Error manually handle karna padta hai
// const res = await fetch(url);
// const data = await res.json();
// 🔹 Axios
// External library
// JSON automatic parse
// Better error handling
// Interceptors support
// const res = await axios.get(url);
// console.log(res.data);
// 🔥 Difference (1 line)

// Axios = easy + powerful, Fetch = basic + manual

// 📌 2. Token Setup (JWT)
// 🔹 Backend (token generate)
// const token = jwt.sign({ id: user._id }, "secret");
// res.json({ token });
// 🔹 Frontend (store token)
// localStorage.setItem("token", token);
// 🔹 Send token in request
// axios.get("/api/posts", {
//   headers: {
//     Authorization: localStorage.getItem("token")
//   }
// });
// 🔹 Backend (verify token)
// const token = req.headers.authorization;
// jwt.verify(token, "secret");
// 🔥 Flow

// 👉 Backend → token deta hai
// 👉 Frontend → store karta hai
// 👉 Request me → header me bhejta hai

// 📌 3. Cookies Setup
// 🔹 Backend (set cookie)
// res.cookie("token", token, {
//   httpOnly: true,
//   secure: true
// });
// 🔹 Frontend (send request)
// Axios:
// axios.get("/api/posts", {
//   withCredentials: true
// });
// Fetch:
// fetch("/api/posts", {
//   credentials: "include"
// });
// 🔹 Backend (read cookie)
// const token = req.cookies.token;
// 📌 4. Token vs Cookie Flow (Important 🔥)
// 🔹 Token (localStorage)
// Frontend → token store
// Frontend → manually header me bhejta hai
// Backend → verify karta hai
// 🔹 Cookie
// Backend → cookie set karta hai
// Browser → automatically store karta hai
// Browser → automatically request me bhejta hai
// Backend → read + verify
// 📌 5. Difference (Token vs Cookie)
// Feature	Token (localStorage)	Cookie
// Send	Manual	Automatic
// Security	❌ less	✅ more (httpOnly)
// Control	Frontend	Backend
// 🧠 Interview Golden Lines
// "JWT can be stored in localStorage or cookies"
// "Authorization header is used for token-based auth"
// "Cookies are automatically sent with requests"
// "httpOnly cookies prevent XSS attacks"
// ⚡ Final Revision (1 minute)

// 👉 Axios > Fetch (easy + features)
// 👉 Token → header me manually
// 👉 Cookie → auto send hoti hai
// 👉 Backend → verify karta hai


